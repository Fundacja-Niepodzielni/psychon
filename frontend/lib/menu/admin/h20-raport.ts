import type { MenuEntry } from "../types";

/** Pakiet H20 — raport edycji. */
const entry: MenuEntry = {
  label: "Raport",
  href: "/admin/raport",
  order: 100,
  icon: "chart",
  section: "raporty",
};

export default entry;
