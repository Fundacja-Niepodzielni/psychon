import type { MenuEntry } from "../types";

/** Pakiet H08 — CMS kursów i lekcji. */
const entry: MenuEntry = {
  label: "Kursy",
  href: "/admin/kursy",
  order: 20,
  icon: "book",
  section: "nauka",
};

export default entry;
