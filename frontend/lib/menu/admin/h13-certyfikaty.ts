import type { MenuEntry } from "../types";

/** Pakiet H13 — lista wydanych certyfikatów i unieważnianie z powodem. */
const entry: MenuEntry = {
  label: "Certyfikaty",
  href: "/admin/certyfikaty",
  order: 45,
  icon: "award",
  section: "osoby",
};

export default entry;
