import type { MenuEntry } from "../types";

/** Placeholder pakietu H19 (pulpit administracji). */
const entry: MenuEntry = {
  label: "Pulpit",
  href: "/admin",
  order: 10,
};

export default entry;
