import type { MenuEntry } from "../types";

/** Pakiet H12 — sprawy zgłoszone administracji przez prowadzących. */
const entry: MenuEntry = {
  label: "Sprawy",
  href: "/admin/sprawy",
  order: 80,
  icon: "folder",
  section: "obsluga",
};

export default entry;
