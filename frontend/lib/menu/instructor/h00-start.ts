import type { MenuEntry } from "../types";

/** Wpis startowy panelu prowadzącego (starter). */
const entry: MenuEntry = {
  label: "Start",
  href: "/prowadzacy",
  order: 10,
};

export default entry;
