import type { MenuEntry } from "../types";

/** Pakiet H12 — terminy superwizji uczestniczki. */
const entry: MenuEntry = {
  label: "Superwizje",
  href: "/panel/superwizja",
  order: 35,
};

export default entry;
